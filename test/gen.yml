Insert:
  import:
    - "os"
  func: |
    func Insert{{.TypeName}}(data *{{.TypeName}}) {
      // TypeName          = {{.TypeName}}
      // TableName         = {{.TableName}}
      // TableColumns      = {{joinq .TableColumns}}
      // NonLabeledColumns = {{joinq .NonLabeledColumns}}
      // LabeledColumns    = {{.LabeledColumns}}
      // ToFieldName       = {{.ToFieldName}}
    }
